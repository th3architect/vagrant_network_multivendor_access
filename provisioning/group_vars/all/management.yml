management:
   api:
      protocol:
         - "http localhost"
         - "unix-socket"
      enabled: True
      vrf:
         - name: "MGMT"
           enabled: True
   cvx:
      enabled: True
      server: "10.255.1.100"
      source_interface: "Management1"
   ssh:
      authentication: "password"
      vrf:
         - name: "MGMT"
           enabled: True
